require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe ProvinciasController do

  # This should return the minimal set of attributes required to create a valid
  # Provincia. As you add validations to Provincia, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    {}
  end

  describe "GET index" do
    it "assigns all provincias as @provincias" do
      provincia = Provincia.create! valid_attributes
      get :index
      assigns(:provincias).should eq([provincia])
    end
  end

  describe "GET show" do
    it "assigns the requested provincia as @provincia" do
      provincia = Provincia.create! valid_attributes
      get :show, :id => provincia.id.to_s
      assigns(:provincia).should eq(provincia)
    end
  end

  describe "GET new" do
    it "assigns a new provincia as @provincia" do
      get :new
      assigns(:provincia).should be_a_new(Provincia)
    end
  end

  describe "GET edit" do
    it "assigns the requested provincia as @provincia" do
      provincia = Provincia.create! valid_attributes
      get :edit, :id => provincia.id.to_s
      assigns(:provincia).should eq(provincia)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new Provincia" do
        expect {
          post :create, :provincia => valid_attributes
        }.to change(Provincia, :count).by(1)
      end

      it "assigns a newly created provincia as @provincia" do
        post :create, :provincia => valid_attributes
        assigns(:provincia).should be_a(Provincia)
        assigns(:provincia).should be_persisted
      end

      it "redirects to the created provincia" do
        post :create, :provincia => valid_attributes
        response.should redirect_to(Provincia.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved provincia as @provincia" do
        # Trigger the behavior that occurs when invalid params are submitted
        Provincia.any_instance.stub(:save).and_return(false)
        post :create, :provincia => {}
        assigns(:provincia).should be_a_new(Provincia)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        Provincia.any_instance.stub(:save).and_return(false)
        post :create, :provincia => {}
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested provincia" do
        provincia = Provincia.create! valid_attributes
        # Assuming there are no other provincias in the database, this
        # specifies that the Provincia created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        Provincia.any_instance.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, :id => provincia.id, :provincia => {'these' => 'params'}
      end

      it "assigns the requested provincia as @provincia" do
        provincia = Provincia.create! valid_attributes
        put :update, :id => provincia.id, :provincia => valid_attributes
        assigns(:provincia).should eq(provincia)
      end

      it "redirects to the provincia" do
        provincia = Provincia.create! valid_attributes
        put :update, :id => provincia.id, :provincia => valid_attributes
        response.should redirect_to(provincia)
      end
    end

    describe "with invalid params" do
      it "assigns the provincia as @provincia" do
        provincia = Provincia.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        Provincia.any_instance.stub(:save).and_return(false)
        put :update, :id => provincia.id.to_s, :provincia => {}
        assigns(:provincia).should eq(provincia)
      end

      it "re-renders the 'edit' template" do
        provincia = Provincia.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        Provincia.any_instance.stub(:save).and_return(false)
        put :update, :id => provincia.id.to_s, :provincia => {}
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested provincia" do
      provincia = Provincia.create! valid_attributes
      expect {
        delete :destroy, :id => provincia.id.to_s
      }.to change(Provincia, :count).by(-1)
    end

    it "redirects to the provincias list" do
      provincia = Provincia.create! valid_attributes
      delete :destroy, :id => provincia.id.to_s
      response.should redirect_to(provincias_url)
    end
  end

end
